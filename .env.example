# OpenAI API key used by the food agent to extract structured search
# parameters from the driver's command (gpt-4o-mini).
OPENAI_API_KEY=your-openai-api-key

# HERE API key used by the food agent for geocoding and the Discover
# (restaurant search) endpoints.
HERE_API_KEY=your-here-api-key
